import React from 'react';
import Box from "@mui/material/Box";
import Typography from "@mui/material/Typography"
const Title = () => {
    return (
        <Box marginTop={3} >
            <Typography variant="h3" gutterBottom>Blogs app</Typography>
        </Box>
    );
};

export default Title;